using TVRegDiff
using Test

@testset "TVRegDiff.jl" begin
    # TVDiff(a, 2, 0.5, u_0=[0;diff(a);0], scale="small")
    # TVDiff(a, 2, 0.5, u_0=[0;diff(a);0], scale="large")
    # TVDiff(a, 2, 0.5, u_0=[0;diff(a)], scale="small")
    # TVDiff(a, 2, 0.5, u_0=[0;diff(a)], scale="large")
end